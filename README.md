# How to setup backup files in Docker
1. 

# How to set up DB backups
1. Clone project on host machine, i.e into `/srv/backuper`
2. Go inside project folder and execute `sudo bash setup-job.sh config.yaml`

